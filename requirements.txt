# STM32 Edge Vision — unified requirements
#
# Covers both the PC-side Reference Dashboard (PySide6 GUI) and the
# ML training / TFLite verification workflow.
#
# Python 3.10 or 3.11 recommended (TensorFlow 2.16 requires Python ≤ 3.11).
#
# Quick start:
#   python -m venv .venv
#   .venv\Scripts\activate        # Windows
#   source .venv/bin/activate     # Linux / macOS
#   pip install -r requirements.txt

# ── GUI (Reference Dashboard) ────────────────────────────────────────────────
PySide6>=6.6.0          # Qt widgets + QtSerialPort (included)
pyserial>=3.5           # fallback serial access / port enumeration

# ── Image handling ───────────────────────────────────────────────────────────
Pillow>=10.3.0

# ── Deep Learning (training + TFLite inference) ──────────────────────────────
tensorflow==2.16.2      # training, TFLite converter, PTQ quantisation
# Lighter alternative for inference only (no training):
# ai-edge-litert>=1.0.1

# ── Data / numerics ──────────────────────────────────────────────────────────
numpy>=1.24.0,<2.0      # TF 2.16 is not compatible with numpy 2.x
pandas>=2.0.0           # CSV export for benchmark results

# ── Experiment tracking (ST Model Zoo MLflow integration) ────────────────────
mlflow>=2.11.0

# ── Visualisation ────────────────────────────────────────────────────────────
matplotlib>=3.8.0

# ── YAML / Hydra config (ST Model Zoo) ──────────────────────────────────────
pyyaml>=6.0
omegaconf>=2.3.0

# ── Utilities ────────────────────────────────────────────────────────────────
tqdm>=4.66.0
