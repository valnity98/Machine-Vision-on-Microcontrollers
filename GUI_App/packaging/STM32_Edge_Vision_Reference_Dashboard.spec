# -*- mode: python ; coding: utf-8 -*-

a = Analysis(
    ['app/src/main.py'],
    pathex=['app/src'],
    binaries=[],
    datas=[('app/ui/reference_window.ui', 'ui'), ('app/assets/app_icon.ico', 'assets')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='STM32_Edge_Vision_Reference_Dashboard',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=['app/assets/app_icon.ico'],
)
